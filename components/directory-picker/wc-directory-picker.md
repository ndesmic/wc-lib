# Wc-directory-picker

A button that provides a directory picker and stores it in indexed-db

## Features

- Click to open picker, retains handle if supplied otherwise it's ephemeral.

## Specs

## TODO

- [x] retain handle
- [x] ephemeral
- [ ] optionally import idb (is there a sane way to do this?)

## Notes

Should it actually hold a reference or just open the picker dialog?